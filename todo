数据库组织原则，重构 （需要把相近的定理放在一起）

引入idea，类型类的概念

在数据库中引入trajectory的概念（从同一聊天历史出来的应该在同一trajectory）

出论文

progress do not write theorem

Ideation skill 只在side task触发

skill需要描述清楚怎么set up project，init

A `VERIFIED` marker is usable only when it matches the current cached statement, dependency snapshot, and verification record.

似乎还没添加import block

proof block must follow the theorem immediately


提供skill，帮助用户创建main Theorem

条件开发，基于某些结果开发、写论文

把命令的help写清楚


你不要过度设计啊，DISPROVED就是个标签，后面依赖啥都都随便写。DISPROVED不一定写反例、反向的证明，或者很明确的直觉都可以。另外这个标签


README.md: 区分给人读的与给ai读的。要加上自动推送，自动更新的prompt


要做成plugin吗？marketplace安装吗？


verifier, 对于stale变化不用查的太严，可以参考上一次的历史版本。


paper是验证正确、notes是验证可读，notes需要definition下面引用足够多examples

用git action做各个安装的测试

写documentation, 检查skill agents.md

inspect把运行时间打印出来，要不然设定一个运行时间，每次超过返回控制权给ai。

请把verifier的prompt调好，并且添加足够多设置，比如思考强度，是否允许工具等等。

跟他说，就像见了一门新的编程语言，check就是debugger，写作的时候就像编程一样，写一些check一下。


跟ai写清楚修改的原理，改一个会影响多少，提醒他注意verifier cost